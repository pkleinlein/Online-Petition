<div class ="container">
    <div class="innerContainer">
        <form method="POST">
            <input type="text" name="loginEmail" id="emailLog" placeholder="email">
            <input type="password" name="loginPass" id="passwordLog" placeholder="password">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <button type="submit" id="buttonLog">Log In</button>
        </form>
    </div>
</div>
